import React from 'react';
<% if (props.themeDir){ %>import ThemeProvider from '<%= props.themeDir%>/ThemeProvider'
<%}%>
import <%= props.name %> from './<%= props.name %>';

export default {
  title: '<% if (props.storyGroup) { %><%= props.storyGroup %>|<% } %><%= props.name %>',
  parameters: {
    componentSubtitle: 'Component subtitle (brief description of its purpose)'
  },
  <% if (props.themeDir){ %>decorators: [
    (storyFn: any) => <ThemeProvider>{storyFn()}</ThemeProvider>,
  ],<%}%>
  component: <%= props.name %>,
};

export const Story = () => {
  return (
    <<%= props.name %> requiredNumberProp={42} />
  );
};